name: Benchmark regional ice-out probability

on:
  workflow_dispatch:
  push:
    branches:
      - seasonal-physics
    paths:
      - scripts/benchmark_regional_probability.py
      - .github/workflows/benchmark-regional-probability.yml

permissions:
  contents: write

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run regional probability benchmark
        run: python scripts/benchmark_regional_probability.py
      - name: Commit report
        run: |
          git config user.name "ice-out-validation-bot"
          git config user.email "actions@users.noreply.github.com"
          git add north-america/data/ice-history/regional-probability-validation.json
          if git diff --cached --quiet; then echo "No changes"; else
            git commit -m "Update regional probability validation"
            git push origin HEAD:${{ github.ref_name }}
          fi
